---
title: "Reordenar colunas"
openapi: "PATCH /v2/workspace/{workspaceId}/tables/{tableId}/columns/reorder"
---

Reordena as colunas da tabela. O array `columnIds` deve conter **todos** os IDs das colunas existentes na tabela, a ordem enviada define a nova posição de cada uma.
